#include <stdio.h>


int main() {
   int charged_cycles;
  scanf("%d",&charged_cycles)
    if(charged_cycles<400)
      printf("low");
  elseif(charged_cycles<=919)
  printf("medium");
  else
    printf("High");    
  return 0;
}
